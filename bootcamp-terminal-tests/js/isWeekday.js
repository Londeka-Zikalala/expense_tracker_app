export default  function isWeekday(weekdays){
    if(weekdays.startsWith('M')||weekdays.startsWith('T')||weekdays.startsWith('W')||weekdays.startsWith('F') ){
       return true}
        else if (weekdays.startsWith('S')||weekdays.startsWith(''))
        {
            return false
        }
    }
    
